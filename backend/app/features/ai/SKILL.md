# AI Feature

Use this feature to check whether the local AI wrapper has an OpenAI API key and model configured.

API endpoints:
- `GET /ai/status`

Other features call AI helpers internally. Missing API keys do not block core app workflows.
