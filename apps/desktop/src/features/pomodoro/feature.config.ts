import type { DesktopFeature } from "@/app/featureRegistry";

const feature: DesktopFeature = {
  key: "pomodoro",
  name: "Focus Operations",
  sidebar: [{ label: "Focus Operations", path: "/pomodoro", icon: "FO" }],
};

export default feature;
